
### Alterar o nome do modulo que aparece em tela

```
SELECT * FROM dicionario.DicionarioCampo WHERE valor LIKE '%Estado do relé de autodiagnóstico%'

UPDATE dicionario.DicionarioCampo SET Valor = 'Estado do relé de autodiagnóstico' WHERE Idioma = 1 AND Chave = 'FE64FA5E-D3B5-4E36-8D5E-09C04C16BB2B

```
